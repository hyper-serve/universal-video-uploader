---
title: Adapters
description: Adapters connect the library to your video backend.
---

import { LinkCard } from "@astrojs/starlight/components";

An adapter handles the actual file upload: authenticating with your backend, tracking progress, and returning a video ID when done. Every `UploadProvider` requires one.

Pass an `UploadConfig` containing your adapter to `UploadProvider`:

```tsx
import { UploadProvider } from "@hyperserve/video-uploader";

<UploadProvider config={config}>
  {/* your upload UI */}
</UploadProvider>
```

The adapter is responsible for the upload itself. The library handles everything around it: queueing, concurrency, retries, and UI state.

## Official adapter

<LinkCard
  title="Hyperserve"
  description="First-party adapter for the Hyperserve video API. The recommended starting point for new projects."
  href="/adapters/hyperserve-adapter/"
/>

Building for a different backend? See [Custom Adapter](/adapters/custom-adapter/).
