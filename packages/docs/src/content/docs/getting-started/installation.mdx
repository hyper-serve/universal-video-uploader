---
title: Installation
description: Install Universal Video Uploader in your project.
---

import { Tabs, TabItem } from "@astrojs/starlight/components";

## Packages

| Package | Purpose |
|---------|---------|
| `@hyperserve/upload` | Core — hooks, state machine, validation, shared theme |
| `@hyperserve/upload-react` | Web UI — DropZone, FileList, FileItem, etc. |
| `@hyperserve/upload-react-native` | React Native UI — FilePicker, FileList, FileItem, etc. |
| `@hyperserve/upload-adapter-hyperserve` | Official Hyperserve adapter |

The core package is always required. Pick the UI package for your platform, or use neither for fully headless usage.

## Web (React)

<Tabs>
  <TabItem label="npm">
    ```bash
    npm install @hyperserve/upload @hyperserve/upload-react
    ```
  </TabItem>
  <TabItem label="yarn">
    ```bash
    yarn add @hyperserve/upload @hyperserve/upload-react
    ```
  </TabItem>
  <TabItem label="pnpm">
    ```bash
    pnpm add @hyperserve/upload @hyperserve/upload-react
    ```
  </TabItem>
  <TabItem label="bun">
    ```bash
    bun add @hyperserve/upload @hyperserve/upload-react
    ```
  </TabItem>
</Tabs>

**Peer dependencies:** React 18+ and React DOM 18+.

## React Native

<Tabs>
  <TabItem label="npm">
    ```bash
    npm install @hyperserve/upload @hyperserve/upload-react-native
    ```
  </TabItem>
  <TabItem label="yarn">
    ```bash
    yarn add @hyperserve/upload @hyperserve/upload-react-native
    ```
  </TabItem>
  <TabItem label="pnpm">
    ```bash
    pnpm add @hyperserve/upload @hyperserve/upload-react-native
    ```
  </TabItem>
  <TabItem label="bun">
    ```bash
    bun add @hyperserve/upload @hyperserve/upload-react-native
    ```
  </TabItem>
</Tabs>

**Peer dependencies:** React 18+.

### Optional native dependencies

All three are optional peer dependencies. The library gracefully degrades without them.

| Package | Purpose | Without it |
|---------|---------|------------|
| `expo-video-metadata` | `maxDuration` validation | Skips validation, warns once |
| `expo-video-thumbnails` | Thumbnail generation | `thumbnailUri` stays `null` |
| `react-native-background-upload` | True background upload with progress | Falls back to `fetch` with estimated progress |

## Hyperserve Adapter

If you are uploading to Hyperserve, install the official adapter package:

<Tabs>
  <TabItem label="npm">
    ```bash
    npm install @hyperserve/upload-adapter-hyperserve
    ```
  </TabItem>
  <TabItem label="yarn">
    ```bash
    yarn add @hyperserve/upload-adapter-hyperserve
    ```
  </TabItem>
  <TabItem label="pnpm">
    ```bash
    pnpm add @hyperserve/upload-adapter-hyperserve
    ```
  </TabItem>
  <TabItem label="bun">
    ```bash
    bun add @hyperserve/upload-adapter-hyperserve
    ```
  </TabItem>
</Tabs>
