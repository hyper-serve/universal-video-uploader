import {
	themeColors,
	themeRadius,
	themeFontScale,
	themeSpacingScale,
} from "@hyper-serve/upload";

export const colors = themeColors;

export const radius = themeRadius;

export const fontSize = themeFontScale;

export const spacing = themeSpacingScale;
